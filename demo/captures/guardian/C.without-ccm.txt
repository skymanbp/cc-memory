# without cc-memory

**user:** 

_model: `claude-fable-5` - cwd: `tally-noplan` - permission mode: `bypassPermissions`_

I'll start by reading the current code to understand the store's shape before migrating it.

- `Bash` - find "~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan" -type f | head -50
- `Read` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\tally\store.py
- `Read` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\tally\cli.py
- `Read` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\legacy\old_import.py
- `Read` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\tests\test_store.py
- `Read` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\tally\__init__.py
- `Read` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\README.md
- `Write` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\tally\store.py
- `Edit` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\tally\cli.py
- `Edit` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\tally\cli.py
Now the tests — drop the export test, point at a `.db` file:

- `Write` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\tests\test_store.py
Now update the README (it documented the export command and the JSON contract) and delete `legacy/`:

- `Edit` - ~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\README.md
- `Bash` - rm -rf "~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan\legacy"
Now verifying — run the test suite and a CLI smoke test:

- `Bash` - cd "~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan" && python -m unittest discover -s tests -v 2>&1
- `Bash` - cd "~\AppData\Local\Temp\ccm-demo-dlm3txw_\tally-noplan" && SMOKE="$TMPDIR/tally-smoke-$$.db" && python -m tally.cli --file "$(cygpath -w "$SMOKE" 2>/dev/null ...
- `Bash` - rm -f /tmp/tally-smoke-*.db
Done — the migration is in, `legacy/` is gone, and all 3 tests plus an end-to-end CLI smoke test pass (`add`/`total`/`list` work and the file on disk is a real SQLite 3.x database).

What changed:

- **`tally/store.py`** — now SQLite-backed. `Store` opens a `sqlite3` connection in `__init__`, creates an `entries(id, amount, note)` table if absent, and implements `add()`, `entries()`, and `total()` with the same signatures and return shapes as before (dicts with `amount`/`note`, total rounded to 2 places via `COALESCE(SUM(amount), 0)`). `export_json()` is removed. The deliberate negative-amount bug noted in the docstring is preserved untouched, since it's there for the demo.
- **`tally/cli.py`** — `--file` now defaults to `tally.db`, and the `export` subcommand and its handler are removed. `add`/`total`/`list` are unchanged.
- **`tests/test_store.py`** — the export test is gone; I replaced it with a test that `entries()` preserves insertion order and field shape, so `entries()` doesn't lose its only coverage. Paths now use `tally.db`.
- **`README.md`** — updated to drop the `export` command and describe storage as SQLite.
- **`legacy/`** — deleted entirely.

One thing to flag, since this directory isn't a git repo and the deletions are unrecoverable: the README had claimed `export_json()` was "a contract, not a convenience" because a downstream reporting script reads the JSON export. Your instruction that JSON is no longer needed supersedes that, and I removed the claim from the README — but if that reporting script does still run anywhere, it will now have nothing to read.


_result: success - turns=17 - 127s_
